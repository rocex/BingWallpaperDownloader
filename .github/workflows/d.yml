name: 自动编译

# 触发条件：推送到 main 分支、或向 main 提交 PR 时触发
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-windows:
    runs-on: windows-latest # 使用 Windows 最新版运行环境
    steps:
      # 1. 检出仓库代码
      - name: 检出代码
        uses: actions/checkout@v5

      
